package com.jad.product.composite;

import com.jad.product.Product;

public class CompositeProduct extends Product {
    public CompositeProduct(final String name, final int height, final int width, final String description) {
        super(name, height, width, description);
    }
}
